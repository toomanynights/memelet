# Memelet Configuration Example
# Copy this file to .env and adjust the values for your installation

# Installation paths
SCRIPT_DIR=/path/to/memelet
MEMES_DIR=/path/to/memelet/files
DB_PATH=/path/to/memelet/memelet.db
LOG_DIR=/path/to/memelet/logs
VENV_DIR=/path/to/memelet/venv

# Web server
BASE_URL=http://localhost:5000
MEMES_URL_BASE=http://localhost:5000/files/
HOST=127.0.0.1
PORT=5000

# Timezone (see https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=UTC

# Security (generate a random secret key for production)
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-to-a-random-value

# API Keys (optional - can also be set in the web UI Settings)
# REPLICATE_API_TOKEN=your_token_here
